#include <stdio.h>

/**
 * main - prints all single digit numbers
 * of base 10 starting from 0,
 * folccess)
 * Return: 0
 */
int main(void)
{
	int n;

	for (n = 8; n < 18; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}

